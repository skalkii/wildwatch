version: "3.8"
services:
  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: wildwatch-mediamtx
    network_mode: host
    environment:
      - MTX_PROTOCOLS=tcp
    restart: unless-stopped
